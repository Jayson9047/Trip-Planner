<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--    Set Image that will be inside the ListView-->
    <ImageView
        android:id="@+id/image_of_hotel"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_launcher_background"/>

    <!--    Set Hotel Name that will be inside the ListView-->
    <TextView
        android:id="@+id/name_of_hotel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="25dp"
        android:textStyle="bold"
        android:layout_toRightOf="@+id/image_of_hotel"
        android:textSize="22sp"/>

    <!--    Set Hotel Cost that will be inside the ListView-->
    <TextView
        android:id="@+id/price_of_hotel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/image_of_hotel"
        android:layout_below="@+id/name_of_hotel"
        android:layout_marginLeft="20dp"
        android:layout_gravity="center"
        android:textSize="18sp"/>

    <!--    Set Hotel Rating that will be inside the ListView-->
    <RatingBar
        android:id="@+id/rating_of_hotel"
        style="?android:attr/ratingBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="150dp"
        android:layout_marginTop="150dp"
        android:layout_alignTop="@+id/name_of_hotel"
        android:layout_toEndOf="@+id/image_of_hotel"
        android:numStars="5"
        android:stepSize="0.5"/>

</RelativeLayout>
